{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(python3 -c \"import sys, json; print(json.load(sys.stdin).get('tool_input', {}).get('file_path', ''))\"); case \"$f\" in */integration_test/*) : ;; */lib/*.dart|*/test/*.dart) flutter test ;; *) : ;; esac",
            "timeout": 120,
            "statusMessage": "Running flutter test (unit/widget)…"
          }
        ]
      }
    ]
  }
}
